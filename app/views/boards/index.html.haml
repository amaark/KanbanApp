%h1.page-heading Listing boards
%br

-# %table
-#   %thead
-#     %tr
-#       %th Board title
-#       %th
-#       %th
-#       %th

-#   %tbody
-#     - @boards.each do |board|
-#       %tr
-#         %td= board.board_title
-#         %td= link_to 'Show', board
-#         %td= link_to 'Edit', edit_board_path(board)
-#         %td= link_to 'Destroy', board, method: :delete, data: { confirm: 'Are you sure?' }

- @boards.each do |board|
  .w3-container
    .w3-card-4{:style => "width:20%;"}
      %header.w3-container.w3-red
        %h1= board.board_title
      .w3-container
        %h4= board.board_description
      %footer.w3-container.w3-red
        %h5= link_to t('show'), board
        %h5= link_to t('edit'), edit_board_path(board)
        %h5= link_to t('destroy'), board, method: :delete, data: { confirm: t('confirmation') }

%br

= link_to t('.new'), new_board_path
